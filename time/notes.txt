there are many simple operations. 
like print ('f')
a= b;
sout a;

these are all like simple operations.  
measn these take a unit time. like 1ms, 1ns, 1us.  but it takes 1 right. means it is constant. 
this are simple operations. 

In leedcode somethime we get time limitation.
for example if it is set in the leedcode that only 100 operatiosn should take place but our program takes 1010 opeartion then that erro will come. "TIME LIMIT EXCEED".

ok So in leedcde there is fixed that we can do 10*8 simple operation in 1 sec. 2 sec 3 sec. based on diffrent langauges. 

ok so in the leedcode there is input given that n = 10;
but as dicusses before that leedocde has only 10* 8 operaion allowed in 1 sec. 

so if the completiy is 0(n) then it is okay. beaucse it is mathcing. 
but what is complexity is 0(n2) then it exceeed. so it will be give error. 

what ever the input is given we need to write insed of that operation only. 
and if the input is n*3 then we cant wirte not n*2 also.


when we take output as the result. o(n-1) now these is the expression. 
so (n raise to 1 and 1 becomes n raise to 0) so highest becomes n raise to 1.  

so time completime becomes n rasie to 1. ie 0(n)

in the expression whatever the big n is ther that will be its time compexity.  
ie o(n rasie to 2 + o rasie to 1 + o rasie to 0);
so complexity becomes to o(n rasie to 2)

So the time complexity is the highest of the section of the code. 